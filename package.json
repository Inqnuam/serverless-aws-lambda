{
  "name": "serverless-alb-offline",
  "version": "1.0.6",
  "description": "AWS Application Load Balancer mock for Serverless",
  "main": "dist/index.js",
  "author": "Inqnuam",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/inqnuam/serverless-alb-offline.git"
  },
  "peerDependencies": {
    "esbuild": "^0.15.5",
    "esbuild-node-externals": "^1.4.1"
  },
  "devDependencies": {
    "@types/node": "^18.7.6"
  },
  "scripts": {
    "build": "./node_modules/.bin/esbuild ./src/index.js --bundle --minify --platform=node --outdir=dist --external:./src/lib/worker.js --external:\"./node_modules/*\" && ./node_modules/.bin/esbuild ./src/lib/worker.js --bundle --minify --platform=node --outdir=dist"
  }
}
